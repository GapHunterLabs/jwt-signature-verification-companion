<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.jwtsignatureverificationcompanion</id>
    <name>JWT Signature Verification Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on a jjwt <code>Jwts.parser()...parse(token)</code>
        call chain -- jjwt's own <code>parse</code> method accepts a JWT
        whose signature is empty or doesn't match, even when a signing key
        has been configured on the parser. This is a documented real
        vulnerability class: CodeQL ships a dedicated "Missing JWT
        signature check" query for exactly this pattern, and jjwt's own
        issue tracker confirms it. The fix is simple but easy to miss --
        use <code>parseClaimsJws()</code>, <code>parseSignedClaims()</code>,
        or <code>parsePlaintextJws()</code> instead, which actually verify
        the signature.</p>
        <p><b>100% static text/PSI analysis</b> -- matches the call chain by
        simple text ("jwt" + "parser"/"parserBuilder" in the receiver, plus
        a bare <code>parse(...)</code> call), so it works whether the real
        jjwt jar is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 targets jjwt specifically (the most common
        Java JWT library) and matches by simple text, not real type
        resolution -- an unrelated <code>.parse(...)</code> method on a
        variable that happens to be named/called like a JWT parser is a
        possible (rare) false positive, and an aliased or wrapped jjwt
        parser call isn't covered.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.jwtsignatureverificationcompanion.gutter.UnverifiedParseLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.jwtsignatureverificationcompanion.gutter.UnverifiedParseLineMarkerProvider"/>
        <notificationGroup id="JWT Signature Verification Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
